# Postgres (Neon)
DATABASE_URL=

# Auth.js v5 — Google provider with workspace-domain restriction
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_ALLOWED_DOMAIN=urjwan.com

# Vercel KV — upload validate→commit handoff (10 min TTL)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Vercel Blob — thumbnail storage
BLOB_READ_WRITE_TOKEN=
