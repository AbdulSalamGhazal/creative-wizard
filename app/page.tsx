export default function Home() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center p-12 gap-6 text-center">
      <h1 className="font-display text-6xl tracking-tight">Urjwan CCMS</h1>
      <p className="text-ink-2 max-w-md">
        Scaffold ready. The dashboard, library, upload, and admin routes are
        next — see <code className="font-mono text-accent-2">docs/tech-spec.md</code>{" "}
        for the full plan.
      </p>
    </main>
  );
}
